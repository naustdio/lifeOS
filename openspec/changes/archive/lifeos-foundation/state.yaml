# DAG state for change `lifeos-foundation` (survives compaction).
# Updated by sdd-apply as sub-slices land; tracker-normalized 2026-08-04
# (2C marked complete, next_recommended = verify).

change: lifeos-foundation
schema: spec-driven

apply_progress:
  sub_slice_1A:
    name: "Scaffold, Design System, Boundary Lint"
    status: complete
    tasks_done: [T-001, T-002, T-003, T-004, T-005, T-006, T-007, T-008]
    tasks_total: 8
    branch: feat/lifeos-foundation-1a-scaffold
    commits:
      - sha_ref: HEAD~1 # feat(scaffold): initialize Next.js/TS/Tailwind scaffold with ESLint module boundaries
        covers: [T-001, T-002, T-003]
        authored_lines: 458
      - sha_ref: HEAD # feat(design-system): tokens, retokenized components, theme selection, PWA shell
        covers: [T-004, T-005, T-006, T-007, T-008]
        authored_lines: 1075
    verify_gate: "pnpm verify — eslint + tsc --noEmit + check-tokens.mjs + next build — PASS"
    test_gate: "pnpm test (vitest) — 4/4 tests pass (boundary-lint smoke test + 3 theme-selection scenarios) — PASS"
    review_budget_note: >
      Cached session budget is 800 changed lines. Total authored diff for
      1A is ~1533 lines (excludes generated pnpm-lock.yaml, binary icon
      assets, and pre-existing openspec artifacts), split across two
      commits on one branch per tasks.md's own recommended (a)/(b) split.
      Commit (a) ~458 lines is within budget; commit (b) ~1075 lines
      exceeds the ~600-850 forecast band, driven by the full retokenized
      component set + patterns + PWA shell landing together. Flagged for
      the user/orchestrator to decide whether commit (b) needs a further
      split into two PRs before opening one, since a single branch was
      requested for the whole 1A sub-slice.
    deviations:
      - "T-007 (mobile-first layout harness) shipped as a manual checklist
         (tests/e2e/mobile-first-checklist.md) with structural reasoning,
         not an automated Playwright assertion — full automation is wired
         in at T-040 (sub-slice 2C) once real screens exist, per the
         task's own 'Playwright or manual checklist' allowance."
      - "shared/{result,money}.ts (listed in design.md's File Changes
         table) were NOT created in 1A — they belong to sub-slices
         1B/2A per tasks.md's own T-001..T-008 scope and were correctly
         out of scope here."
  sub_slice_1B:
    name: "Identity Kernel"
    status: complete
    tasks_done: [T-009, T-010, T-011, T-012, T-013, T-014, T-015, T-016, T-017]
    tasks_total: 9
    branch: feat/lifeos-foundation-1b-identity
    started_from: pr/1a-2-design-system-pwa
    commits:
      - sha_ref: HEAD~1 # feat(core): core schema, RLS membership helpers, and personal-space bootstrap
        covers: [T-009, T-010, T-011, T-012, T-013, T-016]
        authored_lines: 558
      - sha_ref: HEAD # feat(identity): Google OAuth sign-in flow, session middleware, authenticated shell
        covers: [T-014, T-015, T-017]
        authored_lines: 504
    verify_gate: "pnpm verify — eslint + tsc --noEmit + check-tokens.mjs + next build — PASS"
    test_gate: "pnpm test (vitest) — 5/5 tests pass (boundary-lint smoke test, 3 theme-selection scenarios, no-household-text scan) — PASS"
    db_gate: "EXECUTED 2026-08-04 — Supabase CLI installed, local stack started (project_id LIFE_OS, ports remapped to the 55300s to avoid colliding with another project's stack on this machine). All 16 pgTAP assertions across 010_core_rls.sql and 020_core_bootstrap_idempotency.sql pass for real (one initial assertion corrected: anon lacks USAGE on schema core entirely, a stricter guarantee than the original RLS-filtered-to-zero-rows assumption). scripts/test-bootstrap-race.sh also executed for real via two genuinely concurrent connections (docker exec into the db container, since psql is not installed on this machine) — PASS, both callers landed in the same household, no duplicate rows."
    review_budget_note: >
      Cached session budget is 800 changed lines. Total authored diff for
      1B is 1027 lines (excludes pnpm-lock.yaml), exceeding both the single
      PR budget and tasks.md's own ~700-950 forecast band for this
      sub-slice. Split across two commits on one branch mirroring
      tasks.md's own recommended (a)/(b) split: commit (a) 558 lines
      (T-009-T-013 DB migrations + bootstrap + T-016 pgTAP/race-script,
      within the ~350-450 forecast), commit (b) 504 lines (T-014/T-015 auth
      wiring + T-017 UI shell + tasks.md checkboxes, within the ~400-500
      forecast). Shipped as two separate PRs (pr/1b-1-core-schema-rls,
      pr/1b-2-google-oauth), both merged to main.
    deviations:
      - "T-016's true-concurrency case (two callers racing
         core.ensure_personal_space()) cannot be expressed inside a single
         pgTAP file (one transaction per file) — split into a sequential
         idempotency pgTAP test (supabase/tests/020_...) plus a standalone
         bash script opening two real concurrent connections
         (scripts/test-bootstrap-race.sh). Both required together to claim
         the trap is covered; both executed for real, both PASS."
      - "Google OAuth is not yet configured in the Supabase project's Auth
         settings (user-deferred to the end of this cycle) — the
         sign-in/callback code path is real and complete but cannot
         complete a live login until that configuration lands."
      - "src/app/page.tsx (the 1A scaffold placeholder home screen) was
         removed and its content folded into src/app/(app)/page.tsx, since
         Next.js cannot resolve two page.tsx files to the same `/` route
         once the (app) route group's own page.tsx exists."
  sub_slice_2A:
    name: "Finance Schema + Security + API Seam"
    status: complete
    tasks_done: [T-018, T-019, T-020, T-021, T-022, T-023, T-024, T-025, T-026, T-027, T-028, T-029, T-030, T-031, T-032, T-033, T-034, T-035]
    tasks_total: 18
    branch: feat/lifeos-foundation-2a-finance-schema
    started_from: pr/1b-2-google-oauth (merged to main; branch rebuilt clean on main before PR split)
    db_gate: "EXECUTED 2026-08-04 — 83/83 pgTAP assertions pass for real against the local stack (finance schema RLS, derived-balance views with security_invoker regression check, transfer sum-zero invariant, idempotency replay, create_account atomic detail-row insert + positive-opening-balance rejection, SECURITY DEFINER recursion guard, cross-space move rejection, edit-voided rejection, sibling-category-name collision, non-MXN currency CHECK). Independently re-verified twice by sdd-verify (verify-report-2a.md, verify-report-2a-gaps.md), both PASS with 0 CRITICAL."
    test_gate: "pnpm test — 35/35 pass (unit tests + real HTTP integration tests against local Supabase via tests/integration/finance-facade.test.ts). pnpm verify PASS."
    notes: "Folds in most of the originally-separate sub_slice_2B scope (the API seam and TS facade) per the apply agent's task grouping. A real pre-existing bug was found and fixed during gap closure: mapPgError() could not distinguish TRANSFER_LEG_NOT_MOVABLE from VOID_TRANSACTION_NOT_EDITABLE (both raise sqlstate 22023), making the latter unreachable dead code until fixed."
  sub_slice_2C:
    name: "Minimal Finance UI"
    status: complete
    tasks_done: [T-036, T-037, T-038, T-039, T-040]
    tasks_total: 5
    branch: feat/lifeos-foundation-2c-finance-ui
    started_from: pr/2a-3-finance-schema (merged to main; branch rebuilt clean on main before PR split)
    commits:
      - sha_ref: e020af8 # feat(finance): read repositories + minimal account screen (T-036) (#7)
        covers: [T-036]
      - sha_ref: dff5d1c # feat(finance): transaction entry + correction screens (T-037, T-038) (#8)
        covers: [T-037, T-038]
      - sha_ref: 263bf6e # feat(finance): wire real balance hero, RTL render tests, verify report (T-039, T-040) (#9)
        covers: [T-039, T-040]
    verify_gate: "pnpm verify — clean PASS (eslint + tsc --noEmit + check-tokens.mjs + next build, 10 routes generated) after rm -rf .next — PASS"
    test_gate: "pnpm test (vitest) — 43/43 pass at 2C report time (7 files); 52/52 after the C-1 gap-closure run (3 new RTL render tests) — PASS"
    db_gate: "supabase test db — Files=7, Tests=83, Result: PASS — unchanged from 2A/2B (zero migration changes in 2C, no regression)"
    review_budget_note: >
      2C shipped as 3 separate PRs (#7, #8, #9) merged to main, each
      independently reviewable — a finer split than tasks.md's "single PR
      is acceptable" forecast, so no 800-line budget pressure arose.
      Verified by sdd-verify (verify-report-2c.md): PASS WITH WARNINGS;
      0 CRITICAL remaining after C-1 gap closure.
    deviations:
      - "T-040 (E2E smoke suite) delivered as manual checklist
         (tests/e2e/finance-ui-smoke-checklist.md) + integration tests,
         not Playwright — orchestrator-waived for this run, precedent
         T-007 ('manual checklist or Playwright'). C-1 (no component had
         ever been rendered) closed via 3 RTL render tests; W-2/S-2
         (unexecuted checklist rows) accepted by the orchestrator as
         carried-forward risk (verify-report-2c.md §8)."
      - "Reads routed through finance/api instead of finance/data — forced
         by the actual ESLint Gate A allow list (app may import only
         module-api/design-system/shared); legitimate constraint, residual
         design-hygiene tension tracked as verify-report-2c W-1."
      - "CreateAccountInput/RecordTransactionInput/RecordTransferInput
         widened from z.infer to z.input — correct Zod fix for
         default-bearing fields, confirmed by verify-report-2c (not scope
         drift)."
      - "no-household-text.test.ts regex narrowed to word boundaries —
         correct fix excluding internal identifiers (householdId etc.),
         confirmed by verify-report-2c."
      - "Transfer-leg-reject remedy simplified (void both legs, no
         auto-redirect/prefill) — honestly-scoped narrowing of design.md's
         Open Question, error path handled end-to-end (verify-report-2c
         deviation (d); brittle substring match tracked as W-3)."

next_recommended: archived
# Rationale: all 40 tasks (T-001..T-040) delivered across 9 merged PRs; final cycle
# verification (1A-2C) PASSED WITH WARNINGS (0 CRITICAL) per verify-report.md
# (2026-08-05, fresh re-executed evidence: pnpm verify clean, 83/83 pgTAP, 52/52 vitest).
# Google OAuth provider configuration landed in commit e8f43fd and was confirmed working
# by the user via a real local sign-in.
#
# CLOSURE NOTE (2026-08-05): archived MANUALLY, not via the native gentle-ai review/archive
# gate. The native review gate could not complete in this session because the review-*
# subagent definitions expose no Bash tool, so gentle-ai review inspect-candidate (which
# needs shell access to the frozen Git trees) could never run; all 4 lenses honestly
# reported inspection: incomplete rather than fabricating a result. The dispatcher then
# kept verify/archive blocked pending a gentle-ai.verify-result/v1 envelope and that same
# bounded review transaction. The user explicitly directed a manual close based on the
# real, independently re-executed test evidence instead of pursuing the native gate. Full
# detail in archive-report.md.
archived: true
archived_date: "2026-08-05"
