// `finance` domain layer — pure TypeScript, zero Supabase/framework imports.
//
// Sub-slice 1A ships only the folder shape. Money domain rules (signed
// amounts, transfer-pair construction, balance arithmetic) land in
// sub-slice 2A.
export {};
