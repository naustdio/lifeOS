// `core` domain layer — pure TypeScript, zero Supabase/framework imports.
//
// Sub-slice 1A ships only the folder shape (module-architecture spec's
// "Module Folder Structure" requirement). Real entities (profile, household,
// membership invariants) land in sub-slice 1B alongside the `core` schema
// migrations.
export {};
