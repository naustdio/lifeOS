// `core` data layer — Supabase repositories and row <-> entity mappers.
//
// Sub-slice 1A ships only the folder shape. Real repositories land in
// sub-slice 1B once the `core` schema and Supabase clients exist.
export {};
